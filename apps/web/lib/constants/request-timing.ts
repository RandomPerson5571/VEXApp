/** Day-plan toggles, task status changes. */
export const TOGGLE_MUTATION_DELAY_MS = 300;

/** Admin table inline field saves. */
export const ADMIN_INLINE_SAVE_DELAY_MS = 400;

/** Scouting markdown editor autosave. */
export const SCOUTING_AUTOSAVE_DELAY_MS = 800;

/** Picklist reorder / cross-off before DB sync. */
export const SCOUTING_PICKLIST_DELAY_MS = 300;
