services:
  - type: web
    name: vex-bot
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @stlvex/database db:generate
    startCommand: pnpm --filter bot start
    healthCheckPath: /api/health
